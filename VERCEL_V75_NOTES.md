# V75

- Removed decorative product-card image overlay layers (`.visual::before` / `.visual::after`).
- Normalized product image centering for desktop and mobile: flex centering, `object-position:center center`, no padding/filter/opacity/transform.
- Kept wishlist button and product badge above the image.
- Added CSS cache-busting `style.css?v=75` to the generated homepage/templates.
- No product facts or affiliate URLs were fabricated or changed.

const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const p=JSON.parse(fs.readFileSync(path.join(root,'products.json'),'utf8'));const errs=[];const seen=new Set();for(const [i,x] of p.entries()){for(const k of ['slug','name','category','summary'])if(!x[k])errs.push(`Product ${i+1}: missing ${k}`);if(seen.has(x.slug))errs.push(`Duplicate slug: ${x.slug}`);seen.add(x.slug);if(x.slug&&!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(x.slug))errs.push(`Invalid slug: ${x.slug}`);if(x.affiliateUrl&&x.affiliateUrl.startsWith('PASTE_'))console.warn(`Affiliate URL not filled: ${x.slug}`)}if(errs.length){console.error(errs.join('\n'));process.exit(1)}console.log(`Validation OK: ${p.length} products.`)
